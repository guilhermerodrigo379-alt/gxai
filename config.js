// IMPORTANTE: Este arquivo foi intencionalmente atualizado para promover melhores práticas de segurança.
// NUNCA coloque sua chave de API diretamente neste ou em qualquer outro arquivo do código-fonte.

// A chave de API do Google AI Studio deve ser fornecida exclusivamente através 
// da variável de ambiente `process.env.API_KEY`.

// A aplicação já está configurada para ler esta variável automaticamente.
// Isso evita que chaves secretas sejam acidentalmente expostas em repositórios públicos como o GitHub.

// Como configurar a variável de ambiente:
// 1. Crie um arquivo `.env` na raiz do seu projeto (se não existir).
// 2. Adicione a seguinte linha a ele:
//    API_KEY=SUA_CHAVE_DE_API_VAI_AQUI
// 3. Certifique-se de que seu ambiente de desenvolvimento (como Vite, Next.js, etc.) esteja configurado para carregar variáveis de ambiente.
// 4. NUNCA envie o arquivo `.env` para o seu repositório Git. Adicione `.env` ao seu arquivo `.gitignore`.
